spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB
      file-size-threshold: 2KB
  application:
    name: course-service
  minio:
    bucket-name: master-image
    endpoint: http://127.0.0.1:9000
    access-key: minioadmin
    secret-key: minioadmin