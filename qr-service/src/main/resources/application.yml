qr:
  charset: UTF-8
spring:
  cloud:
    openfeign:
      client:
        config:
          storage_service:
            url: http://localhost:7777/api/v1/storage/
  application:
    name: qr-service
eureka:
  client:
    service-url:
      defaultZone: https://server-akerke.fibo.cloud:8761/eureka
server:
  port: 9999
  servlet:
    context-path: /api/v1/qr/
